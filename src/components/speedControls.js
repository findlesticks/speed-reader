import React from 'react';
import { StyledButton } from './toggleButton';

export default function PlayButton({ speedUp, slowDown, wps }) {
  return <StyledButton onClick={onClick} selected={playing}>{innerText}</StyledButton>
}
